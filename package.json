{
  "name": "openclaw-google-calendar",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw Google Calendar plugin scaffold with skills, tools, and plugin metadata.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "files": [
    "src",
    "skills",
    "docs",
    "examples",
    "openclaw.plugin.json",
    "README.md"
  ],
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test tests/tool-registration.test.mjs tests/google-calendar-gateway.test.mjs tests/google-calendar-auth.test.mjs"
  },
  "dependencies": {
    "googleapis": "^171.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3"
  }
}
