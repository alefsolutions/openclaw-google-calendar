import { plannedGoogleCalendarTools } from "./planned-tools.js";

export function registerGoogleCalendarTools(_api: unknown): void {
  // Structural placeholder only. Real api.registerTool(...) calls will be added
  // once the auth flow, schemas, and Google Calendar operations are implemented.
  void plannedGoogleCalendarTools;
}
